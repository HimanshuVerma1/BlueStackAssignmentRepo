pipeline {

	agent any
	
	parameters {
		gitParameter branchFilter: 'origin/(.*)', 
					 defaultValue: 'RC_Branch',
					 name: 'BRANCH',
					 type: 'PT_BRANCH'
	}
	
	stages {
		stage ('Check out the specific branch') {
			steps{
				git branch: "$(params.BRANCH)", url: 'https://github.com/HimanshuVerma1/BlueStackAssignmentRepo.git'
			}
		}
	}
}